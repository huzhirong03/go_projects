# Excel 拆合大师 · 冒烟测试清单

> 6 个 fixture 覆盖**性能 / 业务 / 边界**三个维度的核心场景。逐项验证完，
> 基本能保证发布版本质量过硬。

## 准备

```powershell
# 1. 生成全部 fixture（约 5-10 秒，产物 ~11 MB）
go run ./cmd/gen-smoke-fixture

# 2. 输出在
explorer testdata_smoke\
```

> 也可只生成指定项：`go run ./cmd/gen-smoke-fixture -only 02,04`

---

## ✅ Fixture 01：10 万行学生信息（流式读性能）

**文件**：`testdata_smoke/01_学生信息_10万行.xlsx`（6.4 MB，单 Sheet）

**埋的关键词**（`年级` 列）：
- `六年级` 约 8500 行
- `高三` 约 8500 行
- `重点班` 1000 行（在班级列）
- `示范学校` 100 行（在学校类型列）

### 测试步骤

| 操作 | 期望 |
|---|---|
| 1. 选 **批量提取 → 单文件** → 选这个 xlsx | 表头预览正常显示 14 列 |
| 2. 关键词 `六年级`，模式 **包含** | 任务完成，命中 ~8500 行 |
| 3. 输出策略 **合成一个文件** | 输出 1 个 xlsx，**不报"部分内容有问题"** |
| 4. 关键词 `六年级,重点班`（多关键词） | 命中数应 ≥ 单独"六年级" |
| 5. 用拼音匹配 `lns`（六年级首字母 `lnj`，错误） | 不命中（验证拼音不乱匹配） |
| 6. 改成大文件提取，**观察日志** | "处理 1 个文件，共 6 MB"banner 应出现（日志面板） |

### 性能基线
- 全表扫描应 **≤ 5 秒**（10 万行流式读）
- 内存峰值 **≤ 200 MB**（流式不应 OOM）

---

## ✅ Fixture 02：共享公式回归测（最关键 ⚠️）

**文件**：`testdata_smoke/02_订单含公式_3万行.xlsx`（1.35 MB）

**核心**：本 fixture 故意用 `<f t="shared" si="0" ref="H2:H30001">D2*E2</f>` 触发**共享公式**机制 + 包含 calcChain.xml。这是上次 commit `a0bc3a7` 修的 bug。

**埋的关键词**：
- `VIP 客户`（B 列）：~600 行
- `促销`（F/G 列）：~1000 行
- `退货`（G 列）：60 行

### 测试步骤

| 操作 | 期望 |
|---|---|
| 1. **提取 `VIP 客户`** → 合成一个文件 | 输出 ~600 行 |
| 2. **打开输出 xlsx** | ⭐ **不弹"部分内容有问题"对话框** |
| 3. 看输出文件的 H 列、I 列、J 列 | 公式还在（金额/折扣/实收都能看到值） |
| 4. 提取 `退货` → 每个关键词一个文件 | 输出 1 个文件，60 行 |
| 5. 拆分 → 按关键词 `VIP 客户,促销,退货` | 输出 3 个文件 |

### 红线
- ❌ Excel 弹"部分内容有问题" → bug 回归，需要修
- ❌ 输出文件公式列空了 → 共享公式展开失败
- ❌ 命中数 < 500 → 关键词匹配 bug

---

## ✅ Fixture 03：2 万行带照片（图片迁移性能）

**文件**：`testdata_smoke/03_员工花名册_2万行带照片.xlsx`（1.2 MB）

> 注：图片复用 8 张缓存图，所以文件不大；**但 anchor 数量 = 2 万**，
> 这才是真正考验图片重映射性能的指标。

**埋的关键词**（`部门` 列）：
- `研发部` 5000 行
- `销售部` 5000 行
- `市场部` 4000 行
- `客服部` 3000 行
- `行政部` 3000 行

### 测试步骤

| 操作 | 期望 |
|---|---|
| 1. 提取 `研发部` → 合成一个文件 → **保留图片** | 输出 ~5000 行，每行带照片 |
| 2. 打开输出 xlsx 看 I 列 | 头像显示在每行（不是空白） |
| 3. 提取 2 个部门 `研发部,销售部` | ~10000 行，全部带图 |
| 4. 拆分 → 按关键词 `研发部,销售部,市场部` | 输出 3 个文件，各自带图 |

### 性能基线
- 5000 行带图提取应 **≤ 30 秒**（如果超过 1 分钟说明 O(N²) 退化）

---

## ✅ Fixture 04：多 Sheet 交叉公式（你那个真实 case）

**文件**：`testdata_smoke/04_学校成绩册_多Sheet交叉公式.xlsx`（0.27 MB）

> 复刻你那个 `01_全校成绩册_3000人...xlsx` 的结构：
> 4 个 Sheet（学生明细 / 班级统计 / 年级统计 / 教师列表）+ 跨 sheet 公式 + 图片。
> 回归 commit `38214d9` (单源多 sheet 保留) + `a0bc3a7` (公式 / calcChain)。

**埋的关键词**：
- `六年级`（学生明细 D 列、班级统计 A 列、年级统计 A 列、教师列表 C 列）
- `优秀`（学生明细 L 列，由公式产出）

### 测试步骤

| 操作 | 期望 |
|---|---|
| 1. **提取 `六年级`** → 合成一个文件 → 保留图片 | 输出 1 个 xlsx |
| 2. **打开输出 xlsx** | ⭐ **不弹"部分内容有问题"** |
| 3. 看 Sheet 标签栏 | ⭐ **应有 4 个 Sheet**（**不止 1 个**） |
| 4. 学生明细 Sheet 中"六年级" 命中行数 | ~500 行（带图） |
| 5. 班级统计 Sheet "六年级" 行数 | ~5 行（5 个班） |
| 6. 年级统计 Sheet | 1 行（六年级行） |
| 7. 教师列表 Sheet | ~5 行（六年级老师） |

### 红线
- ❌ 输出只有 1 个 Sheet → 单源多 sheet 修复失效（commit `38214d9` 回归）
- ❌ 命中行数 < 总埋点 -50% → 数据丢失
- ❌ 弹"部分内容有问题" → 公式 / calcChain 修复失效

---

## ✅ Fixture 05：按列值拆分 30 个班

**文件**：`testdata_smoke/05_按列值拆分_30个班.xlsx`（0.39 MB）

**结构**：1.5 万行学生数据，C 列是班级（1班 - 30班，每班 500 行）

### 测试步骤

| 操作 | 期望 |
|---|---|
| 1. **拆分 → 按列值** → 选 `班级` 列 | 任务运行 |
| 2. 输出目录看 | **30 个 xlsx 文件**（1班.xlsx, 2班.xlsx, ..., 30班.xlsx） |
| 3. 任意打开一个 | 500 行数据 + 表头 |
| 4. 用 `命名` 选项加前缀（比如 `班级-`） | 文件名 `班级-1班.xlsx` 等 |
| 5. 拆分 → 写回源文件新 Sheet 模式 | 源文件多 30 个 Sheet |

### 性能基线
- 30 个文件总输出 **≤ 60 秒**

---

## ✅ Fixture 06：5 个供应商文件（多源合并）

**文件**：`testdata_smoke/06_多源/供应商A_华东电子.xlsx` ... 共 5 个，每个 1 万行

**统一表头**：订单号 / 客户 / 产品 / 数量 / 金额 / 备注

**埋的关键词**：
- `VIP`（每个文件约 200 行，5 个文件共 ~1000 行）

### 测试步骤

| 操作 | 期望 |
|---|---|
| 1. **批量提取 → 文件夹** → 选 `06_多源/` | 预览到 5 个文件、6 列表头 |
| 2. 关键词 `VIP` → 合成一个文件 | 输出 1 个 xlsx，~1000 行 |
| 3. 输出文件中订单号前缀 | 应有 A / B / C / D / E 5 种（5 源都合上了） |
| 4. 改用 **每个源文件一个** 输出 | 输出 5 个 xlsx |
| 5. 改用 **每个关键词一个** 输出 | 输出 1 个文件（只有 1 个关键词） |

---

## 🚨 通用红线（任何 fixture 测试都该守）

不论用哪个 fixture，下面任意一项发生 = bug：

| 红线 | 含义 |
|---|---|
| Excel 打开报"部分内容有问题" | 输出文件结构损坏 |
| 命中数据少于埋点 50%+ | 数据丢失 bug |
| 内存涨到 1GB+ | 流式失效，OOM 风险 |
| 单文件提取超过 2 分钟 | 性能严重退化（应在 10 秒内） |
| 进程崩溃 / 黑屏 | 严重 bug |
| 任务取消按钮没反应 | 取消机制坏了 |

---

## 反馈 bug 流程

发现任何红线，操作：

1. 软件菜单 → **📂 日志**
2. 找到最新的 `task-*.log`
3. 把 log 内容 + 截图发给开发者（邮箱 379705723@qq.com）

---

## 测试通过标准

**6 个 fixture 全部通过 ⇒ 软件可以正式发学员**。

如果哪个不通过，对照红线找根因，**修了再发**。
